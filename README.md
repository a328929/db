# tg_search_web

本地 Telegram SQLite 数据库搜索页面（适配你的 `tg_data.db` 结构）。

## 启动

```bash
pip install -r requirements.txt
python app.py
```

打开：`http://127.0.0.1:8890`

## 目录要求

把 `tg_data.db` 放在和 `app.py` 同一目录。
